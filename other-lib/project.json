{
  "name": "other-lib",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "other-lib/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/other-lib"
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs other-lib {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../coverage/other-lib"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["other-lib/**/*.ts", "other-lib/package.json"]
      }
    }
  },
  "tags": []
}
