# other-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build other-lib` to build the library.

## Running unit tests

Run `nx test other-lib` to execute the unit tests via [Jest](https://jestjs.io).
