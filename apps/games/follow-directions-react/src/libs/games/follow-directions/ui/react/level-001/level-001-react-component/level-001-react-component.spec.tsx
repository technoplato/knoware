import { render } from '@testing-library/react';

import Level001ReactComponent from './level-001-react-component';

describe('Level001ReactComponent', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<Level001ReactComponent />);
    // expect(baseElement).toBeTruthy();
  });
});
